# WHAT IS THE DIFFERENCE BETWEEN PREFIX AND POSTFIX?
If we use prefix, the value is incremented/decremented before being evaluated; this process works in reverse for postfix. 
There is no difference between them, if they are used alone. (i++;) and (++i;) have the same effect. 
