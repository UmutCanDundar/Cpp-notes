# WHAT IS AN INLINE FUNCTION?
If a function is inline, the compiler changes the normal calling process and that reduces the execution time and the program size because a separate location is not allocated on the memory for controlling the function code. Inline keyword is a suggestion, which means that the compiler can ignore it
