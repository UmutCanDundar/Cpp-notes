# WHAT IS THE DIFFERENCE BETWEEN STRUCT AND CLASS?

We can simply say that struct is a value type whereas class is a reference type. Hence, struct members are public and are stored on the stack while class members are stored on the heap. Unlike struct, class have 3 access modifiers which are public, protected and private.
