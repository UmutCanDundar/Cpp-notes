# Namespaces in C++

Namespace prevents name collision, especially when your code includes multiple libraries. The entire namespace can be declared as well as all the names in a namespace can be used individually with the “using” directive. 
For example;
    using namespace std; or using std::endl;
