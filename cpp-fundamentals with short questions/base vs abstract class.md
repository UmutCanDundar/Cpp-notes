# WHAT ARE BASE CLASS AND ABSTRACT BASE CLASS?

A base class is the parent class, from which the other classes are derived and inherit members.  An abstract base class is the base class which contains at least one pure virtual function.
