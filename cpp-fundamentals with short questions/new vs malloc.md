# WHAT IS THE DIFFERENCE BETWEEN NEW AND MALLOC()?

Both new and malloc() are used to allocate space on the heap. New is an operator which calls the constructor; however, malloc() is a C standard library function. In addition, new is faster than malloc().
