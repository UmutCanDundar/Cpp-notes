# GRAPH

Consists of vertices(key) and edges(std::unordered_set of values)

Can be implemented adjacency matrix (2-dimensional array) or adjacency list (std::unordered_map) – more efficient

Add Vertex, Add Edge, Remove Edge – O(1)

Remove Vertex – O(n)

Space Complexity – O(n)
