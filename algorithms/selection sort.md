# SELECTION SORT

Look for the index that has min value: Set minIndex to the first item(0) and compare it to other items, if an item is smaller than it, set the index of the smaller one to minIndex, after checking all items, swap the minimum value and the first item. 
Repeat the process for all items. 
Time complexity – O(n^2)
Space complexity – O(1)
