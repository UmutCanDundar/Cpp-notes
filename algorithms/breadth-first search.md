# BREADTH-FIRST SEARCH

Row by row searching

We use queue<Node*>: 

    when we pop a (parent) node, 
    
    we push (its children) into the queue. (For graph: visited and its neighbours)

Time complexity - O(V+E)

Space complexity - O(V)
